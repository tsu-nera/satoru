## 🫀 自律神経系分析(ECG)

{% if ecg.hrv_img %}
### 時間領域解析

![HRV時系列](img/{{ ecg.hrv_img }})

> **時系列の見方**:
> - **RMSSD**: 副交感神経活動の指標。高いほどリラックス状態。
> - **LF/HF Ratio**: 自律神経バランス。1.0未満は副交感神経優位（リラックス）、1.0以上は交感神経優位（緊張）。

{% endif %}
{% if ecg.hrv_freq_img %}
### 周波数領域解析

![HRV周波数解析](img/{{ ecg.hrv_freq_img }})

> **周波数帯域の説明**:
> - **VLF (0.0-0.04 Hz)**: Very Low Frequency - 長期的な調節機構
> - **LF (0.04-0.15 Hz)**: Low Frequency - 交感神経＋副交感神経活動（圧受容体反射）
> - **HF (0.15-0.4 Hz)**: High Frequency - 副交感神経活動（呼吸性洞性不整脈）

{% endif %}
{% if ecg.hrv_time_stats is not none or ecg.hrv_freq_stats is not none %}
### 統計指標

{% if ecg.hrv_time_stats is not none %}
#### Time Domain

{{ ecg.hrv_time_stats|df_to_markdown(floatfmt='.2f', index=False) }}

{% endif %}
{% if ecg.hrv_freq_stats is not none %}
#### Frequency Domain

{{ ecg.hrv_freq_stats|df_to_markdown(index=False) }}

{% endif %}
> **指標の説明**:
> - **時間領域（Time Domain）**: R-R間隔の変動を時間で評価。SDNN/RMSSDが高いほど心拍変動が大きく、リラックス状態。
> - **周波数領域（Frequency Domain）**: 心拍変動を周波数解析。HFは副交感神経、LFは交感神経＋副交感神経の混合。
> - **非線形（Nonlinear）**: Poincaréプロットによる複雑性評価。SD1は短期変動、SD2は長期変動。
{% if ecg.respiratory_period %}
>
> **⚠️ LF/HF比の解釈について**: 呼吸数が{{ ecg.respiratory_period.breathing_rate|number_format(1) }}回/分と非常に遅いため、呼吸性変動がLF帯域（2.4-9回/分）に集中しています。この場合、LF/HF比の従来解釈（>2.5=交感神経優位）は**適用できません**。むしろ深い瞑想状態を示唆しています。
{% else %}
>
> - **LF/HF Ratio**: 1.0未満で副交感神経優位（リラックス）、1.0以上で交感神経優位（ストレス・緊張）が示唆されます。
{% endif %}

{% endif %}
{% if ecg.respiratory_stats is not none %}
### 呼吸指標

{{ ecg.respiratory_stats|df_to_markdown(floatfmt='.1f', index=False, standardize_columns=True) }}

{% endif %}
{% if ecg.rbp_stats is not none and not ecg.rbp_stats.empty %}
#### 共鳴呼吸回数（Resonance Breathing Pace）

{{ ecg.rbp_stats|df_to_markdown(index=False) }}

> **共鳴呼吸（Resonance Breathing）**: 呼吸のペースと心拍の周期が共鳴し、HRV（特にRMSSD）が最大化される呼吸回数。通常4.5-6.5回/分が最適値。

{% endif %}
