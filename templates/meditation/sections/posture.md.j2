## 🏃 姿勢・体動分析（IMU）

{% if posture.motion_img %}
### 体動・心拍数時系列

![体動・心拍数時系列](img/{{ posture.motion_img }})

> **グラフの見方**:
> - **Motion Index**: 重力成分除去後の純粋な動き（小さいほど静止）
> - **Gyro RMS**: 頭部回転の総合指標（小さいほど安定）
> - **Heart Rate**: 心拍数（BPM）

{% endif %}
{% if posture.summary_table is not none %}
### サマリー統計

{{ posture.summary_table|df_to_markdown(index=False) }}

> **指標の説明**:
> - **平均/最大モーション指数**: 重力成分除去後の純粋な動き（論文ベース、小さいほど静止）
> - **ジャイロRMS**: 頭部回転の総合指標（3軸合成、小さいほど安定）
> - **Pitch RMS**: 前後方向の回転運動（小さいほど安定）
> - **Roll RMS**: 左右方向の回転運動（小さいほど安定）
> - **Yaw RMS**: 水平方向の回転運動（脳波との相関実証済、小さいほど安定）

{% endif %}
{% if posture.detail_table is not none %}
### 時系列詳細（3分ごと）

{{ posture.detail_table|df_to_markdown(index=False) }}

{% endif %}
