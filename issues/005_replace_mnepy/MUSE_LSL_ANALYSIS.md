# muse-lsl プロジェクト分析

**分析日**: 2025-11-11
**バージョン**: v2.3.1

---

## 📊 プロジェクト概要

### 基本情報

| 項目 | 内容 |
|------|------|
| **リポジトリ** | https://github.com/alexandrebarachant/muse-lsl |
| **スター数** | 708 ⭐ |
| **フォーク数** | 194 🍴 |
| **コントリビューター** | 20名 |
| **依存プロジェクト** | 139 |
| **最終リリース** | v2.1.0 (2021年2月) |
| **最終更新** | 2024年6月 (v2.3.1) |
| **Python対応** | Python 2.7 & 3.x |
| **DOI** | 10.5281/zenodo.3228861 |

### 評価

**人気度**: ⭐⭐⭐⭐☆ (708スター = コミュニティで広く使われている)

**メンテナンス**: 🟡 **緩やかに継続中**
- 最終更新: 2024年6月（比較的最近）
- 最終リリース: 2021年2月（やや古い）
- 結論: **活発に使われているが、開発ペースは緩やか**

---

## 🎯 muse-lslの目的

### 主な用途

1. **リアルタイムストリーミング** - Museデバイスから脳波をLSL経由でストリーム
2. **ニューロフィードバック** - リアルタイムで脳状態を可視化・フィードバック
3. **リアルタイムBCI** - 脳-コンピュータインターフェースの構築
4. **実験プロトコル** - P300、SSVEP、SSAEP実験の実施

### ユースケース

- **リアルタイム解析**: ライブで脳波を処理
- **ニューロフィードバック訓練**: α波、β波のリアルタイムモニタリング
- **実験データ収集**: タイムスタンプ付きEEGデータの記録

---

## 🏗️ アーキテクチャ

### コアコンポーネント

```
Muse Device (Bluetooth)
    ↓
muselsl stream (LSL出力)
    ↓
pylsl (Lab Streaming Layer)
    ↓
├─ muselsl view (可視化)
├─ muselsl record (CSV記録)
└─ カスタムアプリ (例: neurofeedback.py)
    ↓
    独自FFT → バンドパワー計算
```

### 依存関係

**コア依存**:
```python
install_requires=[
    "bitstring",
    "bleak>=0.18.0",      # Bluetooth LE
    "pygatt",             # Bluetooth backend
    "pandas",
    "scikit-learn",
    "numpy",
    "seaborn",
    "pexpect",
    "pylsl"               # Lab Streaming Layer
]
```

**オプション依存**:
```python
extras_require={
    "Viewer V2": ["mne", "vispy"]  # ← MNEはビューワーのみで使用
}
```

### 重要な発見

✅ **MNE-Pythonはオプション依存**
- コアにMNEは含まれない
- Viewer V2（高度なビューワー）でのみ使用
- つまり、**muse-lslの信号処理は独自実装**

---

## 🔬 信号処理の実装

### 1. バンドパワー計算（examples/utils.py）

**独自FFT実装**:
```python
def compute_band_powers(eegdata, fs):
    # Hamming窓を適用
    w = np.hamming(winSampleLength)
    dataWinCentered = eegdata - np.mean(eegdata, axis=0)
    dataWinCenteredHam = (dataWinCentered.T * w).T

    # FFT計算（numpy.fft）
    NFFT = nextpow2(winSampleLength)
    Y = np.fft.fft(dataWinCenteredHam, n=NFFT, axis=0) / winSampleLength
    PSD = 2 * np.abs(Y[0:int(NFFT / 2), :])

    # バンドパワー計算
    # Delta <4Hz
    meanDelta = np.mean(PSD[ind_delta, :], axis=0)
    # Theta 4-8Hz
    meanTheta = np.mean(PSD[ind_theta, :], axis=0)
    # Alpha 8-12Hz
    meanAlpha = np.mean(PSD[ind_alpha, :], axis=0)
    # Beta 12-30Hz
    meanBeta = np.mean(PSD[ind_beta, :], axis=0)

    # 対数変換
    return np.log10(feature_vector)
```

**評価**:
- ✅ シンプルで高速（リアルタイム向き）
- ⚠️ Welch法ではなく単純FFT（ノイズに弱い）
- ⚠️ MNEの高度な機能は使わない

---

### 2. ノッチフィルタ（examples/utils.py）

**scipy.signal実装**:
```python
from scipy.signal import butter, lfilter, lfilter_zi

# 55-65Hzのノッチフィルタ（60Hz電源ノイズ除去）
NOTCH_B, NOTCH_A = butter(4, np.array([55, 65]) / (256 / 2), btype='bandstop')

def update_buffer(data_buffer, new_data, notch=False, filter_state=None):
    if notch:
        if filter_state is None:
            filter_state = np.tile(lfilter_zi(NOTCH_B, NOTCH_A), ...)
        new_data, filter_state = lfilter(NOTCH_B, NOTCH_A, new_data,
                                         axis=0, zi=filter_state)
    ...
```

**評価**:
- ✅ リアルタイム対応（状態を保持）
- ✅ scipy標準関数を使用
- 💡 **我々のプロジェクトでも参考にできる**

---

### 3. ニューロフィードバック指標（examples/neurofeedback.py）

**バンド比率**:
```python
# Alpha Protocol（リラックス）
alpha_metric = smooth_band_powers[Band.Alpha] / smooth_band_powers[Band.Delta]

# Beta Protocol（集中度 - ADHD向け）
beta_metric = smooth_band_powers[Band.Beta] / smooth_band_powers[Band.Theta]

# Alpha/Theta Protocol（ストレス軽減）
theta_metric = smooth_band_powers[Band.Theta] / smooth_band_powers[Band.Alpha]
```

**評価**:
- ✅ シンプルで解釈しやすい
- 💡 **我々のバンド比率（α/β、β/θ、θ/α）と類似**
- 参考文献: *Neurofeedback: A Comprehensive Review* by Marzbani et al.

---

## 📊 我々のプロジェクトとの比較

### アーキテクチャの違い

| 観点 | muse-lsl | 我々のプロジェクト (satoru) |
|------|----------|----------------------------|
| **目的** | リアルタイムストリーミング・BCI | オフライン分析・レポート生成 |
| **データ入力** | Bluetooth LE（直接接続） | Mind Monitor CSV |
| **信号処理** | numpy.fft（独自実装） | MNE-Python（標準実装） |
| **フィルタリング** | scipy.signal | MNE: raw.filter() |
| **PSD計算** | 単純FFT | MNE: raw.compute_psd()（Welch法） |
| **スペクトログラム** | なし | MNE: tfr_array_morlet() |
| **MNE使用** | オプション（ビューワーのみ） | コア機能で使用 ✓ |
| **出力** | リアルタイムフィードバック | 詳細レポート（Markdown + 画像） |
| **Python** | 2.7 & 3.x | 3.x推奨 |

---

## 🤔 muse-lslを活用すべきか？

### ❌ 推奨しない理由

1. **目的が異なる**
   - muse-lsl: リアルタイム処理
   - 我々: オフライン分析（すでに記録されたCSV）

2. **信号処理の品質**
   - muse-lsl: 単純FFT（速度重視）
   - 我々: MNE-Python（精度重視）

3. **すでに良い実装がある**
   - 我々は既にMNE-Pythonを使用している ✓
   - muse-lslの独自実装に移行する理由がない

4. **依存関係の複雑化**
   - muse-lslはLSL、Bluetooth関連の依存が多い
   - 我々には不要

### ✅ 参考にできる部分

1. **ノッチフィルタの実装**
   - `utils.py:158` の`update_buffer()`関数
   - scipy.signalによる状態付きフィルタリング
   - **我々のPhase 1で実装予定** 👍

2. **ニューロフィードバック指標**
   - バンド比率の解釈（α/Δ、β/θ、θ/α）
   - Marzbani et al.の文献引用
   - **我々の指標と類似・相互検証に有用** 👍

3. **バンド定義**
   - Delta: <4Hz
   - Theta: 4-8Hz
   - Alpha: 8-12Hz
   - Beta: 12-30Hz
   - **我々のFREQ_BANDSと一致している** ✓

---

## 📖 学べる教訓

### 1. アーキテクチャの選択

**muse-lslの選択**:
- リアルタイム → 単純FFT（速度優先）
- ストリーミング → LSL（標準プロトコル）

**我々の選択**:
- オフライン → MNE-Python（精度優先）
- CSVデータ → Pandas + MNE

**結論**: ✅ **適材適所で正しい選択をしている**

---

### 2. コミュニティの重要性

**muse-lslの成功要因**:
- 708スター = 多くの人が使っている
- 139依存プロジェクト = エコシステムの形成
- DOI付き = 学術的な信頼性

**我々のプロジェクトへの示唆**:
- GitHubでの公開を検討？
- ドキュメントの充実
- 再現可能な分析手法

---

### 3. 8年前のコードでも活用されている

**事実**:
- 初回リリース: ~2016年（8年前）
- 最終更新: 2024年6月（最近）
- Python 2.7サポート継続

**示唆**:
- ✅ **良い設計は時代を超える**
- ✅ シンプルなコードは保守しやすい
- ⚠️ ただし、最新のベストプラクティスには追従すべき

---

## 🎯 最終結論

### muse-lsl vs MNE-Python

**我々のプロジェクトには**:

🏆 **MNE-Python を使い続けるべき**

**理由**:
1. ✅ 既にMNE-Pythonを効果的に使用している
2. ✅ オフライン分析に最適
3. ✅ 科学的妥当性が高い（Welch法PSD、Morlet Wavelet）
4. ✅ 豊富な機能（ICA、Epochs、統計解析）
5. ✅ 活発な開発とコミュニティサポート

**muse-lslから学ぶこと**:
- ✅ ノッチフィルタの実装方法
- ✅ ニューロフィードバック指標の解釈
- ✅ バンド比率の妥当性確認

---

## 📚 推奨アクション

### 短期（Phase 1）

1. **ノッチフィルタの実装**
   - muse-lslの`utils.py`を参考に
   - MNE: `raw.notch_filter()`を使用
   - 参考: `tmp/muse-lsl/examples/utils.py:158`

2. **バンド比率の検証**
   - muse-lslのニューロフィードバック指標と比較
   - Marzbani et al.の論文を参照
   - 参考: `tmp/muse-lsl/examples/neurofeedback.py:132-150`

### 長期（今後の検討）

1. **リアルタイム分析の追加**
   - Mind MonitorからのOSCストリーミング対応
   - この場合、muse-lslのアーキテクチャが参考になる

2. **ニューロフィードバック機能**
   - リアルタイムで脳状態を表示
   - muse-lslのneurofeedback.pyを参考に

---

## 🔗 参考リンク

### muse-lsl関連

- **GitHubリポジトリ**: https://github.com/alexandrebarachant/muse-lsl
- **PyPI**: https://pypi.org/project/muselsl/
- **DOI**: https://doi.org/10.5281/zenodo.3228861

### 参考文献

- Marzbani et al. "Neurofeedback: A Comprehensive Review on System Design, Methodology and Clinical Applications"
- Barachant et al. (2019). "muse-lsl". Zenodo.

### 関連プロジェクト

- **EEG Notebooks**: https://github.com/neurotechx/eeg-notebooks
- **NeuroTechX**: https://neurotechx.com/

---

## 📝 まとめ

### キーメッセージ

1. **muse-lslは素晴らしいプロジェクト** ⭐708スター、活発なコミュニティ
2. **しかし我々の目的には不適合** リアルタイム vs オフライン分析
3. **MNE-Pythonを使い続けるべき** 既に正しい選択をしている ✓
4. **参考にできる部分は取り入れる** ノッチフィルタ、ニューロフィードバック指標

### 次のステップ

→ [PLAN.md](./PLAN.md) Phase 1に進む
→ ノッチフィルタの実装（muse-lsl参考）

---

**作成日**: 2025-11-11
**分析者**: @tsu-nera
